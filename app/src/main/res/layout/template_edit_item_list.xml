<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.zerter.teamconnect.Controlers.MyEditText
        android:layout_marginTop="5dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/templateEditText"
        android:textColor="@color/textColor"
        android:textSize="18sp"
        app:customFont="fonts/Oswald-Light.ttf"
        android:padding="3dp"
        android:layout_toStartOf="@+id/buttonDeleteTemplate"
        android:hint="@string/write_your_template"
        android:background="@color/colorPrimaryDark"/>

    <ImageButton
        android:id="@+id/buttonDeleteTemplate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?android:selectableItemBackground"
        app:srcCompat="@android:drawable/ic_menu_delete" />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/textColor"
        android:layout_below="@+id/templateEditText"
        android:layout_marginTop="5dp"/>
</RelativeLayout>